import { SingleLetterDirective } from './single-letter-directive';

describe('SingleLetterDirective', () => {
  it('should create an instance', () => {
    const directive = new SingleLetterDirective();
    expect(directive).toBeTruthy();
  });
});
